import { Helmet } from "react-helmet";

 
const Profile = () => {
    return (
        <div>
            <Helmet>
                <title>Profile</title>
            </Helmet>
            Profile
        </div>
    );
};

export default Profile;